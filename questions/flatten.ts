// Take a nested list and return a single flattened list with all values except nil/null.
// The challenge is to write a function that accepts an arbitrarily-deep nested list-like structure
// and returns a flattened structure without any nil/null values.

// EG:
// input: [1,[2,3,null,4],[null],5]
// output: [1,2,3,4,5]

// Note: the input array will never be double nested for the base acceptance criteria
// Bonus: How could it be improved to handle infinite nesting?

export const flatten = (arr: Array<any>): Array<number> => {
  return [];
};
