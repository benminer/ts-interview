// Mode: The most frequent number—that is, the number that occurs the highest number of times.
// Example: The mode of {4 , 2, 4, 3, 2, 2} is 2
// because it occurs three times, which is more than any other number.
export const mode = (numbers: number[]) => {
  return;
};

// Mean: average of the numbers: a calculated "central" value of a set of numbers
export const mean = (numbers: number[]) => {
  return;
};

// Median: the middle value in a list of numbers
// Note: If the list has an even number of elements, the median is the average of the two middle elements.
export const median = (numbers: number[]) => {};
